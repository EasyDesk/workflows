name: Continuous Integration

on:
  workflow_call:
    inputs:
      dotnet-version:
        type: string
        required: true
      warnings-as-errors:
        type: boolean
        required: false
        default: false
      tests:
        type: string
        required: false
        description: >
          JSON array of test strings.
          Each string: <project_path> [optional_args]
          Example: |
            tests:
              - tests/Project.UnitTests/Project.UnitTests.csproj
              - "tests/My Test Project/Project.IntegrationTests.csproj" --filter "Category=Integration"
        default: '[]'
      build-artifact-name:
        type: string
        required: true
      project:
        type: string
        required: true
      submodules:
        description: >
          true: checkout submodules
          false: don't checkout submodules
          recursive: recursively checkout submodules
        required: false
        default: 'false'
        type: string
    outputs:
      version:
        value: ${{ jobs.publish.outputs.version }}
      should-release:
        value: ${{ jobs.publish.outputs.should-release }}
      is-prerelease:
        value: ${{ jobs.publish.outputs.is-prerelease }}
      major:
        value: ${{ jobs.publish.outputs.major }}
      minor:
        value: ${{ jobs.publish.outputs.minor }}
      patch:
        value: ${{ jobs.publish.outputs.patch }}


defaults:
  run:
    shell: bash


env:
  NUGET_PACKAGES: ${{ github.workspace }}/.nuget/packages


jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: EasyDesk/action-semver-checkout@v1
        with:
          submodules: ${{ inputs.submodules }}
      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: ${{ inputs.dotnet-version }}
          cache: true
          cache-dependency-path: '**/packages.lock.json'
      - name: Build
        uses: EasyDesk/action-dotnet-build@v1
        with:
          path: ${{ inputs.project }}
          warnings-as-errors: ${{ inputs.warnings-as-errors }}

  test:
    name: Test
    needs: build
    runs-on: ubuntu-latest
    if: ${{ inputs.tests != '[]' }}
    strategy:
      fail-fast: false
      matrix:
        test: ${{ fromJson(inputs.tests) }}
    steps:
      - name: Checkout
        uses: EasyDesk/action-semver-checkout@v1
        with:
          submodules: ${{ inputs.submodules }}
      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: ${{ inputs.dotnet-version }}
          cache: true
          cache-dependency-path: '**/packages.lock.json'
      - name: Run Tests
        run: |
          dotnet test --project ${{ matrix.test }}
        shell: bash

  publish:
    name: Publish
    needs:
      - build
      - test
    if: ${{ !cancelled() && !failure() }}
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.semver.outputs.version }}
      should-release: ${{ steps.semver.outputs.should-release }}
      is-prerelease: ${{ steps.semver.outputs.is-github-prerelease }}
      major: ${{ steps.semver.outputs.major }}
      minor: ${{ steps.semver.outputs.minor }}
      patch: ${{ steps.semver.outputs.patch }}
    steps:
      - id: semver
        name: Checkout
        uses: EasyDesk/action-semver-checkout@v1
        with:
          submodules: ${{ inputs.submodules }}
      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: ${{ inputs.dotnet-version }}
          cache: true
          cache-dependency-path: '**/packages.lock.json'
      - name: Create temporary directory
        id: tempdir
        run: |
          echo "tempdir=$(mktemp -d)" >> $GITHUB_OUTPUT
      - name: Publish
        uses: EasyDesk/action-dotnet-publish@v1
        with:
          path: ${{ inputs.project }}
          output-dir: ${{ steps.tempdir.outputs.tempdir }}
      - uses: actions/upload-artifact@v7
        with:
          name: ${{ inputs.build-artifact-name }}
          path: ${{ steps.tempdir.outputs.tempdir }}
